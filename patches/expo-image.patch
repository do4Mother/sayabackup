diff --git a/build/Image.types.d.ts b/build/Image.types.d.ts
index 022ae487e65ae9d624f8a4be262b01944928f250..606f6437e4499727830f53888060b7d910c83f92 100644
--- a/build/Image.types.d.ts
+++ b/build/Image.types.d.ts
@@ -322,7 +322,19 @@ export interface ImageProps extends Omit<ViewProps, 'style' | 'children'> {
      * @platform ios
      */
     enforceEarlyResizing?: boolean;
+
+    /**
+   * Specifies the loading behavior of the image.
+   * @platform web
+   */
+  loading?: ImageLazyLoading;
 }
+
+/**
+ * @hidden
+ */
+export type ImageLazyLoading = "lazy" | "eager" | "auto";
+
 /**
  * It narrows down some props to types expected by the native/web side.
  * @hidden
@@ -513,5 +525,5 @@ export type ImageLoadOptions = {
      */
     onError?(error: Error, retry: () => void): void;
 };
-export {};
+export { };
 //# sourceMappingURL=Image.types.d.ts.map
\ No newline at end of file
diff --git a/src/ExpoImage.web.tsx b/src/ExpoImage.web.tsx
index 2a49ff00649b374b86fa780653a4b0d6f13a4a57..cfe097af0732af7b73edf6dd078da8f514985617 100644
--- a/src/ExpoImage.web.tsx
+++ b/src/ExpoImage.web.tsx
@@ -2,7 +2,7 @@ import React from 'react';
 // TODO(@kitten): We shouldn't be importing all of react-native-web or rely on it for a web module in this way optimally
 import { View } from 'react-native-web';
 
-import type { ImageNativeProps, ImageSource, ImageLoadEventData, ImageRef } from './Image.types';
+import type { ImageLoadEventData, ImageNativeProps, ImageRef, ImageSource } from './Image.types';
 import AnimationManager, { AnimationManagerNode } from './web/AnimationManager';
 import ImageWrapper from './web/ImageWrapper';
 import loadStyle from './web/imageStyles';
@@ -78,6 +78,7 @@ export default function ExpoImage({
   alt,
   tintColor,
   containerViewRef,
+  loading,
   ...props
 }: ImageNativeProps) {
   const imagePlaceholderContentFit = placeholderContentFit || 'scale-down';
@@ -151,6 +152,7 @@ export default function ExpoImage({
           priority={priority}
           contentPosition={selectedSource ? contentPosition : { top: '50%', left: '50%' }}
           hashPlaceholderContentPosition={contentPosition}
+          loading={loading}
           hashPlaceholderStyle={imageHashStyle}
           accessibilityLabel={accessibilityLabel}
           tintColor={tintColor}
diff --git a/src/Image.types.ts b/src/Image.types.ts
index 9dec0e7aee61dfaa73a3cfa535d08259c9dad209..debf87ee010f0313f04aa2ad3ff91b0bcd344234 100644
--- a/src/Image.types.ts
+++ b/src/Image.types.ts
@@ -66,6 +66,11 @@ export type ImageSource = {
   isAnimated?: boolean;
 };
 
+/**
+ * @hidden
+ */
+export type ImageLazyLoading = "lazy" | "eager" | "auto";
+
 /**
  * @hidden
  */
@@ -372,6 +377,13 @@ export interface ImageProps extends Omit<ViewProps, 'style' | 'children'> {
    * @platform ios
    */
   enforceEarlyResizing?: boolean;
+
+
+  /**
+   * Specifies the loading behavior of the image.
+   * @platform web
+   */
+  loading?: ImageLazyLoading;
 }
 
 /**
diff --git a/src/web/ImageWrapper.tsx b/src/web/ImageWrapper.tsx
index e8f891d525892f8d3668e34cf96cefccfbfc49f6..bc79a5d5698db37cc2780486d3a4e4b37d9125d0 100644
--- a/src/web/ImageWrapper.tsx
+++ b/src/web/ImageWrapper.tsx
@@ -1,12 +1,12 @@
-import React, { useEffect, Ref, useId } from 'react';
+import React, { Ref, useEffect, useId } from 'react';
 
+import { ImageNativeProps, ImageSource } from '../Image.types';
 import ColorTintFilter, { getTintColorStyle } from './ColorTintFilter';
 import { ImageWrapperProps } from './ImageWrapper.types';
 import { getImageWrapperEventHandler } from './getImageWrapperEventHandler';
 import { useHeaders, useImageHashes } from './hooks';
 import { absoluteFilledPosition, getObjectPositionFromContentPositionObject } from './positioning';
 import { SrcSetSource } from './useSourceSelection';
-import { ImageNativeProps, ImageSource } from '../Image.types';
 
 function getFetchPriorityFromImagePriority(priority: ImageNativeProps['priority'] = 'normal') {
   return priority && ['low', 'high'].includes(priority) ? priority : 'auto';
@@ -36,6 +36,7 @@ const ImageWrapper = React.forwardRef(
       className,
       accessibilityLabel,
       cachePolicy,
+      loading,
       ...props
     }: ImageWrapperProps,
     ref: Ref<HTMLImageElement>
@@ -84,6 +85,7 @@ const ImageWrapper = React.forwardRef(
           fetchPriority={getFetchPriorityFromImagePriority(priority || 'normal')}
           {...getImageWrapperEventHandler(events, sourceWithHeaders)}
           {...getImgPropsFromSource(source)}
+          loading={loading}
           {...props}
         />
       </>
diff --git a/src/web/ImageWrapper.types.ts b/src/web/ImageWrapper.types.ts
index 19bbe2f15999124cda0ca7c81b3ebcf289f522f8..4386ca4939143c822200a67cd23f48afeaa5f154 100644
--- a/src/web/ImageWrapper.types.ts
+++ b/src/web/ImageWrapper.types.ts
@@ -1,7 +1,7 @@
 import { CSSProperties, SyntheticEvent } from 'react';
 
-import { SrcSetSource } from './useSourceSelection';
 import { ImageContentPositionObject, ImageProps, ImageSource } from '../Image.types';
+import { SrcSetSource } from './useSourceSelection';
 
 export type OnErrorEvent =
   | (({ source }: { source: ImageSource | null }) => void)
@@ -35,4 +35,5 @@ export type ImageWrapperProps = {
   className?: string;
   accessibilityLabel?: string;
   cachePolicy?: ImageProps['cachePolicy'];
+  loading?: ImageProps['loading'];
 };
